# Core
numpy
miniaudio
keyboard
pyperclip
httpx

# Audio preprocessing
noisereduce
pedalboard

# Whisper backend
faster-whisper

# Canary / Parakeet backend (NeMo-based, optional)
nemo_toolkit[asr]
silero-vad
soundfile

# Qwen ASR backend (optional — install only if using Qwen)
qwen-asr

# PyTorch with CUDA (install separately):
# uv pip install --reinstall torch torchaudio --index-url https://download.pytorch.org/whl/cu128
